name: whatsapp_chat_ui
description: A minimal Flutter app implementing a WhatsApp-like chat screen.
publish_to: 'none'

environment:
  sdk: '>=2.17.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  # audio recording will use the platform recorder via a MethodChannel

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/
